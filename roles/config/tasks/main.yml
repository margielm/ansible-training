---
- name: create new folder
  file: path=/configuration state=directory owner=vagrant
  become: yes

- name: copy configuration
  template:
    src: "{{file}}"
    dest: "/configuration"
    backup: yes